---
schema_version: nl-proof-v1
artifact_id: NLP-IMP-001-R5
work_item_id: IMP-001
title: Finite-exceptional ordinary-improper integration and Abel composition
owner: jaumededios
revision: 5
artifact_state: draft
proof_status: complete

theorem_card_bindings:
  - card_id: IMP-001
    card_revision: 5
    signature_artifact_id: absent-deferred-until-fresh-r5-proof-and-structural-approvals

target_bindings:
  - target_id: IMP-001-T01
    declaration: prospective LMLF.Integral.HasImproperIntegralAtTopExcept
    proof_section: target-imp-001-t01
    theorem_class: definition
    coverage_class: reusable_infrastructure
    novelty_class: non_novel
  - target_id: IMP-001-T02
    declaration: prospective LMLF.Integral.HasImproperIntegralAtTopExcept.unique
    proof_section: target-imp-001-t02
    theorem_class: existence_uniqueness
    coverage_class: reusable_infrastructure
    novelty_class: non_novel
  - target_id: IMP-001-T03
    declaration: prospective LMLF.Integral.HasImproperIntegralAtTopExcept.finset_clm
    proof_section: target-imp-001-t03
    theorem_class: exact_identity
    coverage_class: reusable_infrastructure
    novelty_class: non_novel
  - target_id: IMP-001-T04
    declaration: prospective LMLF.Integral.HasImproperIntegralAtTopExcept.split_regular
    proof_section: target-imp-001-t04
    theorem_class: exact_identity
    coverage_class: reusable_infrastructure
    novelty_class: non_novel
  - target_id: IMP-001-T05
    declaration: prospective LMLF.Integral.IsFiniteExceptionalPrimitive
    proof_section: target-imp-001-t05
    theorem_class: definition
    coverage_class: reusable_infrastructure
    novelty_class: non_novel
  - target_id: IMP-001-T06
    declaration: prospective LMLF.Integral.IsFiniteExceptionalPrimitive.hasImproperIntegralAtTopExcept_exp_smul
    proof_section: target-imp-001-t06
    theorem_class: exact_identity
    coverage_class: reusable_infrastructure
    novelty_class: non_novel
  - target_id: IMP-001-T07
    declaration: prospective LMLF.Integral.HasImproperIntegralAtTopExcept.of_integrableOn_Ioi
    proof_section: target-imp-001-t07
    theorem_class: qualitative_support
    coverage_class: reusable_infrastructure
    novelty_class: non_novel
  - target_id: IMP-001-T08
    declaration: prospective LMLF.Integral.hasImproperIntegralAtTopExcept_iff_exists_primitive
    proof_section: target-imp-001-t08
    theorem_class: exact_identity
    coverage_class: reusable_infrastructure
    novelty_class: non_novel

source_bindings:
  occurrences: []
  entities: []
  notations: []
  snapshots: []

dependency_bindings:
  lean_toolchain: leanprover/lean4:v4.33.1
  mathlib_revision: 0df444a360eaa60ab8c11dca51a86af692955474
  lake_manifest_sha256: 4bd9b595f84163f69bdb0ffc9b1a876b5df71d65bdd53122415610d083d43404
  accepted_work_items: []

classification_schema_binding:
  path: review/classifications-v3.json
  schema_version: lmlf-classification-v3
  artifact_revision: 3
  sha256: 975e08b89d609cbdd15ff3f8d24f40a42bb17ea0f4c37bb26d9cf53fb8dc4b5e

structural_circularity_review:
  applicability: required
  inapplicable_reason: null
  required_perspectives:
    - dependency_direction
    - source_to_target_reachability
    - hypothesis_and_choice_laundering

supersedes: NLP-IMP-001-R4
---

# IMP-001: finite-exceptional improper integration dossier

## Status and exact boundary

This is the fifth complete author draft for the provisional, unregistered
`IMP-001` infrastructure packet. It is source-independent, earns no Olver
coverage, is not frozen, awaits fresh proof and structural review, and
authorizes no Lean declaration. Every declaration name in the frontmatter is
prospective. Revision 5 preserves the accepted ordinary one-sided endpoint
semantics and the revision-4 public Abel composition, generalizes finite CLM
combinations from (E\to E) to (E\to H), and adds the two public bridges
required by the complete QL/OLV consumer route: absolute Bochner integrability
to the relation (T07), and equivalence with existence of a convergent normalized
finite-exceptional primitive (T08). An R5 exact Lean signature artifact is
deliberately absent and may be designed only after the required fresh reviews.
The revision-3 signature artifact remains unchanged historical evidence and is
not an R5 signature input.

The packet has exactly eight consumer-justified public targets and no hidden quantitative theorem. It fixes
the mathematical meaning of a finite-exceptional ordinary improper integral on
a right ray, fixes its normalized continuous primitive semantics and exact
existential equivalence, proves the three relation laws needed by the Watson
path, bridges honest whole-ray Bochner integrals, and proves one finite-exception
Abel composition theorem at an exact explicitly named value. The private proof
derives the finite-interval identity by Fubini, assembles it across arbitrary
finite `S` through T08, and passes to infinity using two explicit weighted-tail hypotheses.
It does not prove the bounded-primitive tail estimate: that is `QL-001`. It
does not discuss Gamma moments or source recovery.

The interface follows the provisional D1--D5 disposition in
`review/questions/OLV97-C03-WATSON.md`: regular finite pieces are Mathlib
Bochner `intervalIntegral`s with explicit `IntervalIntegrable` evidence, while
only the finite exceptional endpoints and positive infinity are limits. No
second proper-Riemann library and no principal-value convention are introduced.

## Exact target map and classifications

| Target | Mathematical content | Target class | Coverage | Novelty |
|---|---|---|---|---|
| `IMP-001-T01` | relational right-ray ordinary-improper value with finite exceptions | definition | reusable infrastructure | non-novel |
| `IMP-001-T02` | uniqueness of the related value | existence/uniqueness | reusable infrastructure | non-novel |
| `IMP-001-T03` | finite-union refinement and continuous-real-linear combination (E\to H) | exact identity | reusable infrastructure | non-novel |
| `IMP-001-T04` | split/prepend a compact regular interval | exact identity | reusable infrastructure | non-novel |
| `IMP-001-T05` | normalized continuous finite-exceptional primitive predicate | definition | reusable infrastructure | non-novel |
| `IMP-001-T06` | representation-independent finite-exception exponential composition | exact identity | reusable infrastructure | non-novel |
| `IMP-001-T07` | absolute Bochner right-ray integral implies the relation for arbitrary (S) | qualitative support | reusable infrastructure | non-novel |
| `IMP-001-T08` | relation iff existence of a convergent normalized primitive | exact identity | reusable infrastructure | non-novel |

The packet summary is `mixed` only because the current frozen v3 vocabulary places
definitions, uniqueness, qualitative support, and exact identities in different
theorem-shape classes. All eight targets are classical infrastructure and make
no source claim.

The eight targets follow concrete consumers rather than an arbitrary ceiling.
`Basic` owns T01, T05, T08, then T02--T04; `Bochner` imports `Basic` and the
set-integral/improper-limit support needed for T07; `Abel` imports `Basic` and
the finite Fubini, FTC, and set-integral/improper-limit support needed for T06.
The finite-piece Abel identity, ordered recursion, anchors, and regular-point
insertion stay private. QL uses T07 for absolutely integrable model terms, T03
for subtraction on the union of exceptional sets, T04 for removing the local
prefix, T08 to obtain the baseline primitive, and T06 for the new weight.
OLV states its source convergence with T01, uses T08 to obtain (F), and then
derives boundedness from continuity plus the finite limit. No consumer unfolds
the relation or selects a primitive by a public choice function.

## Source and named-object reconciliation

Not applicable. The frontmatter binds no source occurrence, entity, notation,
or snapshot, and none of the eight targets names an Olver function. IMP-001 earns
no source coverage. Its only downstream role is to supply source-independent
integration semantics to QL-001. There is no approximant, truncation order,
branch choice, named normalization, or source exceptional value to reconcile.

## Standing assumptions and quantifier ledger

All integrals are with respect to real Lebesgue volume. Let \(E\) be a complete
normed real vector space and, in T03, let \(H\) be another complete normed real
vector space. Completeness is used for Bochner integration and Fubini;
Hausdorffness, already supplied by the normed structure, is used for uniqueness
of limits. We write \(r\mathbin{\bullet}v\) for real scalar multiplication.

| ID | Binder or data | Type | Quantifier position | Depends on | Mathematical role |
|---|---|---|---:|---|---|
| `IMP-D01` | \(E\) | complete normed real vector space | outermost for all targets | nothing | common input/value space |
| `IMP-D01a` | \(H\) | complete normed real vector space | outermost for T03 | nothing | output/value space after continuous-linear transport |
| `IMP-D02` | \(f\) or \(g\) | \(\mathbb R\to E\) | fixed before endpoints | `IMP-D01` | integrand |
| `IMP-D03` | \(k\) | \(\mathbb R\) | fixed before exceptions | nothing | regular start of the right ray |
| `IMP-D04` | \(S\), or \(S_i\) in T03 | finite subsets of \(\mathbb R\) | fixed after \(k\), before values | every set lies in \((k,\infty)\) | exceptional points |
| `IMP-D05` | \(I,J\) | \(E\), or \(H\) for T03's conclusion | proposed values after \(f,k,S\) | endpoint limits | relational integral values |
| `IMP-D06` | finite index set \(A\) | finite set | after \(f_i,k,S_i\) data | nothing | finite-linearity index and finite union |
| `IMP-D07` | \(T_i\) | continuous real-linear maps \(E\to H\) | one for each \(i\in A\) | `IMP-D01`, `IMP-D01a` | maps applied before integration |
| `IMP-D08` | \(a\le k\) | real endpoints | after right-ray data | regularity on \([a,k]\) | split point for T04 |
| `IMP-D09` | \(F\) | \(\mathbb R\to E\) | explicit input after \(g,k,S\) | continuity and increment law | normalized primitive witness |
| `IMP-D10` | \(a\le b\) | real endpoints | internal to the T06 proof | nothing | regular finite piece for the private Abel lemma |
| `IMP-D11` | \(h\) | \(\mathbb R\) | arbitrary after T06 data | nothing | exponential weight parameter |
| `IMP-D12` | weighted-primitive integrability | `IntegrableOn (fun t => exp(-h*t) • F(t)) (Ioi k)` | explicit T06 input | \(g,k,S,F,h\) | absolute tail control for the ordinary Mathlib value on the right-hand side |
| `IMP-D13` | weighted boundary vanishing | `Tendsto (fun R => exp(-h*R) • F(R)) atTop (nhds 0)` | explicit T06 input | \(F,h\) | removes the terminal Abel boundary term |
| `IMP-D14` | whole-ray absolute integrability | `IntegrableOn f (Ioi k)` | explicit T07 input | \(f,k\) | makes the Bochner value honest and supplies the canonical primitive |
| `IMP-D15` | convergent primitive witness | existential \(F\) satisfying T05 and `Tendsto F atTop (nhds I)` | right side of T08 | \(f,k,S,I\) | representation-independent equivalent semantics, with no norm bound |

T03 uses one common \(k\), permits one finite exceptional set \(S_i\) for each
summand, and concludes in \(H\) on \(S_A=\bigcup_{i\in A}S_i\). T04 fixes the proposed
tail value before prepending the regular interval. T06 is valid for every real
\(h\) for which its two explicit weighted-tail propositions hold, including
zero when they do. The Watson consumer instead derives both propositions from
the separate flat bound on \(F\) and \(h>0\). No datum here depends on a Watson
truncation order. T08 quantifies `∃ F`; it is not an equivalence for a fixed
arbitrary \(F\), and \(F\)'s values below \(k\) are immaterial.

## Precise relational semantics

Write the increasing enumeration of \(S\) as

\[
 c_1<\cdots<c_m,\qquad k<c_1.
\]

This enumeration is unique because \(S\) is a finite set in a linear order.
For a bounded open component \((c_i,c_{i+1})\), use the displayed deterministic
anchor \(d_i=(c_i+c_{i+1})/2\). For the last component after \(c_m\), use
\(d_m=c_m+1\). These formulas are a mathematical presentation, not a demand
that Lean store a sorted list or these anchors.

Every integral below is `intervalIntegral` and is accompanied by
`IntervalIntegrable` on its compact interval. The one-sided filters remain
inside the indicated regular component.

- If \(m=0\), require a value \(I_0\) with

  \[
    \int_k^R f(t)\,dt\longrightarrow I_0\quad(R\to+\infty),
  \]

  and set \(I=I_0\).
- If \(m>0\), require a first-component value \(I_0\) with

  \[
    \int_k^b f(t)\,dt\longrightarrow I_0\quad(b\uparrow c_1).
  \]
- For \(1\le i<m\), require independent values \(L_i,R_i\) with

  \[
   \int_a^{d_i}f(t)\,dt\longrightarrow L_i\quad(a\downarrow c_i),
   \qquad
   \int_{d_i}^{b}f(t)\,dt\longrightarrow R_i\quad(b\uparrow c_{i+1}),
  \]

  and define that component's contribution to be \(L_i+R_i\).
- For the last component, require independent values \(L_m,R_m\) with

  \[
   \int_a^{d_m}f(t)\,dt\longrightarrow L_m\quad(a\downarrow c_m),
   \qquad
   \int_{d_m}^{R}f(t)\,dt\longrightarrow R_m\quad(R\to+\infty),
  \]

  and define its contribution to be \(L_m+R_m\).

The relation `HasImproperIntegralAtTopExcept f k S I` means that all these
regular-piece integrability statements and limits hold and that

\[
 I=I_0+\sum_{i=1}^{m-1}(L_i+R_i)+(L_m+R_m)                 \tag{IMP.1}
\]

when \(m>0\), with the evident empty-set formula when \(m=0\). In particular,
the left and right contributions at \(c_i\) are not limits along a common
cutoff. The value \(f(c_i)\) itself is immaterial, as expected for Lebesgue
finite-piece integrals, but divergent approach from either side is not
immaterial.

The initial endpoint \(k\) is deliberately different from the points of
\(S\). T01 requires `IntervalIntegrable f` on every regular truncation starting
at \(k\). Since singleton values are null-set irrelevant, this accepts an
absolutely integrable endpoint singularity, such as a cutoff version of
\((t-k)^{-1/2}\), but rejects a merely conditionally improper non-L1
singularity at \(k\). This is the local contract needed by QL: its source powers
have exponent greater than \(-1\). Values of \(f\) at \(k\) or in \(S\) do not
affect the relation.

### `IMP-001-I01` - anchor independence

Let \(l<d<e<r\) lie in one regular component. For truncation points
\(l<a<d<e<b<r\), interval additivity gives

\[
 \int_a^e f=\int_a^d f+\int_d^e f,
 \qquad
 \int_e^b f=\int_d^b f-\int_d^e f.                      \tag{IMP.2}
\]

The fixed compact integral \(\int_d^e f\) exists by regular-piece
`IntervalIntegrable`. Passing separately to \(a\downarrow l\) and
\(b\uparrow r\) shows that changing the anchor from \(d\) to \(e\) adds
\(\int_d^e f\) to the left contribution and subtracts the same vector from the
right contribution. Their sum is unchanged. The identical calculation applies
to the final unbounded component; its right endpoint limit is at infinity.
Thus IMP.1 is independent of the displayed anchors. This also proves that an
eventual Lean representation may use a sorted list, adjacent intervals, or a
component record without changing the mathematical relation.

### Optional semantic check - insertion of a regular marked point

This calculation is retained as a non-normative check on T01's endpoint
meaning. R5 does not use it to prove T03 or T04: both public laws factor through
T08. An implementation need not reproduce this four-case argument.

Suppose \(d\notin S\), \(k<d\), and \(d\) lies inside one regular component.
Because \(S\) is finite, choose \(p<d<q\) in that same component. The T01
regularity clause gives `IntervalIntegrable f volume p q`. Define

\[
 P(x)=\int_p^x f(t)\,dt.
\]

The exact pinned theorem
`intervalIntegral.continuousOn_primitive_interval'` (`IMP-M09`), applied with
the anchor \(p\in[[p,q]]\), makes \(P\) continuous on the unoriented interval
\([[p,q]]=[p,q]\). Consequently the restrictions of \(P\) to \((p,d)\) and
\((d,q)\) tend separately to the same value \(P(d)\). For fixed regular
anchors \(u<d<v\) in the old component, interval additivity rewrites the two
variable-endpoint integrals as

\[
 \int_u^x f=P(x)-P(u)\longrightarrow P(d)-P(u)=\int_u^d f
 \quad(x\uparrow d),                                    \tag{IMP.2a}
\]

\[
 \int_x^v f=P(v)-P(x)\longrightarrow P(v)-P(d)=\int_d^v f
 \quad(x\downarrow d).                                  \tag{IMP.2b}
\]

Thus insertion produces the two **individual** one-sided endpoint limits at
\(d\); it does not merely prove that their sum exists. We now compare the old
and refined component decompositions in every possible position.

1. **Insertion into the sole component when \(S=\varnothing\).** The old
   component is \([k,\infty)\). Equation IMP.2a gives the new first-component
   limit \(\int_k^d f\), and IMP.2b gives the finite left contribution of the
   new final component. For any fixed \(v>d\), additivity gives

   \[
    \int_k^R f=\int_k^d f+\int_d^v f+\int_v^R f.
   \]

   The old \(R\to\infty\) limit therefore gives the new final outer limit
   after subtracting the two fixed compact integrals. The two new component
   values sum to the old value.
2. **Insertion into the first component \([k,c_1)\).** Let

   \[
    B=\lim_{x\uparrow c_1}\int_k^x f
   \]

   be the old first-component contribution, and choose an explicit regular
   anchor \(v\) with \(d<v<c_1\). Equation IMP.2a with \(u=k\) gives the new
   first contribution as the individual limit

   \[
    \int_k^x f\longrightarrow\int_k^d f\qquad(x\uparrow d).
   \]

   Equation IMP.2b gives the new bounded component's individual left-at-\(d\)
   value

   \[
    \int_x^v f\longrightarrow\int_d^v f\qquad(x\downarrow d).
   \]

   At its other endpoint, additivity gives the separate right-at-\(c_1\)
   limit

   \[
    \int_v^x f=\int_k^x f-\int_k^v f
      \longrightarrow B-\int_k^v f\qquad(x\uparrow c_1).
   \]

   The new bounded component therefore contributes

   \[
    \int_d^v f+\left(B-\int_k^v f\right)
      =B-\int_k^d f,
   \]

   since \(\int_k^v f=\int_k^d f+\int_d^v f\). Adding the new first
   contribution \(\int_k^d f\) recovers exactly \(B\). Every old limit at
   \(c_1,\ldots,c_m\) and infinity is unchanged.
3. **Insertion into a bounded interior component \((c_i,c_{i+1})\).**
   IMP.2a and IMP.2b provide the new right-of-\(c_i\)-to-\(d\) and
   \(d\)-to-left-of-\(c_{i+1}\) endpoint limits separately. Additivity, or
   equivalently anchor independence IMP-I01, shows that their four finite
   pieces have exactly the old component sum. All outer exceptional limits are
   unchanged.
4. **Insertion into the final component \((c_m,\infty)\).** IMP.2a and IMP.2b
   give both new limits at \(d\). The original \(R\to\infty\) limit, after
   subtracting the fixed integral from the old anchor to any \(v>d\), is the
   new final outer limit. The new bounded and unbounded component values sum to
   the old final contribution; every earlier exceptional limit is unchanged.

In all four cases, regular-piece `IntervalIntegrable` evidence is inherited by
restriction, every new one-sided limit exists individually, every old outer
limit is preserved, and the finite component sum is unchanged. Deleting a
marked point known to be regular reverses the same equalities. This is
background validation only, not a public target or a mandatory proof edge.

## Pinned dependency ledger

Every non-elementary mathematical step is bound below to an exact declaration
and defining import at the pinned Mathlib revision, or is proved internally.
The eventual public signature artifact and its minimal transitive import list
remain intentionally undesigned pending fresh R5 proof-and-architecture
approvals.

| ID | Exact pinned declaration or internal fact | Defining import at the pin | Exact use |
|---|---|---|---|
| `IMP-M01` | `intervalIntegral.integral_add_adjacent_intervals` | `Mathlib.MeasureTheory.Integral.IntervalIntegral.Basic` | IMP.2, splitting, and telescoping |
| `IMP-M02` | `ContinuousLinearMap.intervalIntegral_comp_comm` | `Mathlib.MeasureTheory.Integral.IntervalIntegral.Basic` | move each \(T_i\) through a regular Bochner interval integral |
| `IMP-M03` | `tendsto_nhds_unique` | `Mathlib.Topology.Separation.Hausdorff` | uniqueness of every component limit and total value |
| `IMP-M04` | `tendsto_finsetSum` | `Mathlib.Topology.Algebra.Monoid` | pass finite sums through every endpoint limit |
| `IMP-M05` | `ContinuousOn.intervalIntegrable_of_Icc` | `Mathlib.MeasureTheory.Integral.IntervalIntegral.Basic` | weighted continuity implies finite-piece integrability of \(F\) |
| `IMP-M06` | `MeasureTheory.intervalIntegral_integral_swap` | `Mathlib.MeasureTheory.Integral.Prod` | exchange the two finite triangular integrals in the private finite-piece lemma after proving product integrability |
| `IMP-M07` | `intervalIntegral.integral_eq_sub_of_hasDerivAt_of_le` | `Mathlib.MeasureTheory.Integral.IntervalIntegral.FundThmCalculus` | integrate the derivative of \(t\mapsto e^{-ht}\) on \([u,v]\) |
| `IMP-M08` | `intervalIntegral.integral_undef` | `Mathlib.MeasureTheory.Integral.IntervalIntegral.Basic` | negative regression showing why an integral value alone cannot witness integrability |
| `IMP-M09` | `intervalIntegral.continuousOn_primitive_interval'` | `Mathlib.MeasureTheory.Integral.DominatedConvergence` | optional semantic check for insertion of a regular marked point |
| `IMP-M10` | `Real.hasDerivAt_exp` | `Mathlib.Analysis.SpecialFunctions.ExpDeriv` | chain rule input giving derivative \(-h e^{-ht}\) for IMP-M07 |
| `IMP-M11` | `MeasureTheory.intervalIntegral_tendsto_integral_Ioi` | `Mathlib.MeasureTheory.Integral.IntegralEqImproper` | pass the weighted primitive's finite interval integrals to its ordinary set integral on \((k,\infty)\) |
| `IMP-M12` | `MeasureTheory.IntegrableOn.continuousOn_Ici_primitive_Ioi` | `Mathlib.MeasureTheory.Integral.DominatedConvergence` | continuity on \([k,\infty)\) of the canonical T07 primitive from whole-ray integrability |
| `IMP-M13` | `intervalIntegral.integral_Ioi_sub_Ioi` | `Mathlib.MeasureTheory.Integral.IntervalIntegral.Basic` | identify \(\int_k^t f\) with the difference of two Ioi set integrals and prove the canonical primitive increment law |
| `IMP-M14` | `intervalIntegral.intervalIntegrable_iff_integrableOn_Ioc_of_le` | `Mathlib.MeasureTheory.Integral.IntervalIntegral.Basic` | restrict T07's `IntegrableOn (Ioi k)` premise to honest regular finite intervals |
| `IMP-I01` | anchor independence proved above | this artifact | make the relation independent of auxiliary component anchors |

No future LMLF packet is used. In particular, neither QL-001 nor any Watson
source statement can justify an IMP-001 hypothesis or proof step.

The proof dependency order below is semantic rather than target-number order:
definitions T01 and T05, then the independent equivalence T08, then relation
laws T02--T04, and finally bridges T07 and T06. T08 is proved directly from the
component semantics and never invokes T02, T03, T04, T06, or T07. This is the
edge that prevents the convenience proofs from becoming a cycle.

<a id="target-imp-001-t01"></a>
## IMP-001-T01 - finite-exceptional right-ray relation

T01 is the predicate defined by IMP.1 and the preceding endpoint conditions.
Its complete contract includes all of the following rather than merely the
displayed sum:

1. \(S\subset(k,\infty)\);
2. `IntervalIntegrable f` on every compact subinterval of each component of
   \([k,\infty)\setminus S\);
3. separate one-sided limits at both sides of every \(c\in S\);
4. a one-sided limit at \(+\infty\);
5. the finite sum of component contributions equals the proposed value \(I\).

Because all five clauses are propositions and the value is an explicit
argument, T01 neither totalizes a divergent integral nor chooses a value.
IMP-I01 proves that auxiliary anchors are presentation details. This completes
the definition target.

<a id="target-imp-001-t05"></a>
## IMP-001-T05 - normalized finite-exceptional primitive

For \(f:\mathbb R\to E\), \(k\in\mathbb R\), finite
\(S\subset(k,\infty)\), and explicit \(F:\mathbb R\to E\), define
`IsFiniteExceptionalPrimitive f k S F` to mean:

1. \(F\) is continuous on \([k,\infty)\);
2. \(F(k)=0\);
3. whenever \(k\le a\le b\) and \([a,b]\cap S=\varnothing\), \(f\) is
   `IntervalIntegrable` on \([a,b]\) and

   \[
     F(b)-F(a)=\int_a^b f(v)\,dv.                        \tag{IMP.9}
   \]

This definition includes the condition \(S\subset(k,\infty)\) but no bound on
\(F\), convergence at infinity, or Laplace parameter. Normalization removes
the additive-constant ambiguity without selecting a function: \(F\) is an
explicit argument. Only its restriction to \([k,\infty)\) is constrained.

Continuity has a precise semantic purpose. Fix \(c\in S\), choose regular
anchors \(d<c<e\) in its adjacent components, and apply IMP.9:

\[
 \int_d^b f=F(b)-F(d)\longrightarrow F(c)-F(d)
 \quad(b\uparrow c),                                    \tag{IMP.10}
\]

\[
 \int_a^e f=F(e)-F(a)\longrightarrow F(e)-F(c)
 \quad(a\downarrow c).                                  \tag{IMP.11}
\]

Thus T05 supplies independent finite values at each exceptional point even
when \(f\) is not norm-integrable across it. It supplies no infinity limit.

<a id="target-imp-001-t08"></a>
## IMP-001-T08 - equivalence with a convergent finite-exceptional primitive

For the same \(f,k,S,I\), the public semantic equivalence is

\[
 \operatorname{HasImp}(f,k,S,I)
 \quad\Longleftrightarrow\quad
 \exists F,\ \operatorname{IsFiniteExceptionalPrimitive}(f,k,S,F)
       \ \wedge\ F(t)\longrightarrow I\quad(t\to+\infty).       \tag{IMP.23}
\]

The existential quantifier is part of the target. T08 does **not** say that an
arbitrary fixed \(F\) is equivalent to the relation, and it includes no bound
on \(F\). A witness may be assigned arbitrary values below \(k\).

For the forward direction, use the T01 component values for the increasing
enumeration \(c_1<\cdots<c_m\). If \(m=0\), set
\(F(t)=\int_k^t f\) for \(t\ge k\), set \(F(k)=0\), and extend arbitrarily
below \(k\). The compact integrability clause gives continuity on every
finite interval and the increment law; T01's atTop limit gives \(F\to I\).

Suppose \(m>0\). On the first component put
\(F(t)=\int_k^t f\), and define \(F(c_1)=I_0\), the first left-endpoint
limit. Inductively, suppose \(F(c_i)\) is the total of all completed component
contributions. For an anchor \(d_i\in(c_i,c_{i+1})\), define

\[
 F(t)=F(c_i)+L_i+\int_{d_i}^t f
 \quad(c_i<t<c_{i+1}),                                  \tag{IMP.24a}
\]

and set \(F(c_{i+1})=F(c_i)+L_i+R_i\). On the final
component use the same formula with \(d_m>c_m\). The left endpoint limit of
\(\int_{d_i}^t f\) is \(-L_i\), and its right endpoint limit is \(R_i\), so
these definitions are continuous from both sides at every \(c_i\). Interval
additivity gives IMP.9 inside each component, including the first component.
The endpoint definitions and the independent one-sided limits extend the same
increment law to every closed interval avoiding \(S\). On the final component,
the last atTop limit and the finite telescope say exactly that \(F(t)\to I\).
This constructs a T05 witness without selecting a value by an API-level choice.

For the reverse direction, let \(F\) satisfy T05 and tend to \(I\). T05 gives
every regular compact integrability clause. If \(S=\varnothing\),

\[
 \int_k^R f=F(R)-F(k)=F(R)\longrightarrow I.
\]

If \(S\ne\varnothing\), choose anchors \(d_i\) as in the T01 semantics.
Continuity and IMP.9 give the first value \(F(c_1)\), the bounded-component
values

\[
 L_i=F(d_i)-F(c_i),\qquad
 R_i=F(c_{i+1})-F(d_i),                                 \tag{IMP.24b}
\]

and, on the final component,

\[
 L_m=F(d_m)-F(c_m),\qquad R_m=I-F(d_m).                 \tag{IMP.24c}
\]

Every limit is one-sided and independent. Their finite sum telescopes to
\(I-F(k)=I\), so all clauses of T01 hold. This proof uses no uniqueness theorem
and no absolute-integrability conclusion. It establishes the exact source
semantics that OLV may state canonically as T01 before extracting a primitive.

<a id="target-imp-001-t02"></a>
## IMP-001-T02 - uniqueness

Assume both `HasImproperIntegralAtTopExcept f k S I` and
`HasImproperIntegralAtTopExcept f k S J`. Use the unique increasing enumeration
of \(S\) in both witnesses. On each component, the two witnesses give limits of
the same finite-interval function along the same nontrivial one-sided filter.
By `IMP-M03`, the first contribution is equal in the two witnesses, each left
contribution is equal, each right contribution is equal, and the final
infinite-endpoint contribution is equal. The finite sums in IMP.1 are therefore
equal, so \(I=J\).

This proof uses neither integrability across an exceptional point nor a
principal-value cancellation. The relation is a partial-value relation with at
most one value, but T02 does not define a choice-selected partial-value
function.

<a id="target-imp-001-t03"></a>
## IMP-001-T03 - finite-union refinement and continuous-linear combination

Let \(A\) be a finite index set. For every \(i\in A\), let
\(f_i:\mathbb R\to E\), \(I_i\in E\), a finite
\(S_i\subset(k,\infty)\), and a continuous real-linear map
\(T_i:E\to H\) be given. Put

\[
 S_A=\bigcup_{i\in A}S_i,
\]

which is again a finite subset of \((k,\infty)\), and assume

\[
 \operatorname{HasImp}(f_i,k,S_i,I_i)\qquad(i\in A).    \tag{IMP.3}
\]

The target conclusion is

\[
 \operatorname{HasImp}
 \left(t\mapsto\sum_{i\in A}T_i(f_i(t)), k, S_A,
       \sum_{i\in A}T_i(I_i)\right).                    \tag{IMP.4}
\]

Apply the forward direction of T08 separately to IMP.3. For each \(i\), obtain
an explicit \(F_i\) satisfying T05 for \(S_i\) and
\(F_i(t)\to I_i\). The same \(F_i\) satisfies T05 for the larger set \(S_A\):
every interval avoiding \(S_A\) avoids \(S_i\), and no new claim of
integrability through an old exceptional point is made. Define

\[
 F_A(t)=\sum_{i\in A}T_i(F_i(t)).                       \tag{IMP.3a}
\]

This function is continuous on \([k,\infty)\), is zero at \(k\), and tends to
\(\sum_iT_i(I_i)\) by continuity of the maps and finite-sum limits. On any
compact \(S_A\)-regular interval, each \(f_i\) is `IntervalIntegrable`. A
continuous linear map preserves Bochner integrability, and a finite sum of
integrable functions is integrable. By `IMP-M02` and finite linearity,

\[
 \int_a^b\sum_{i\in A}T_i(f_i(t))\,dt
 =\sum_{i\in A}T_i\left(\int_a^b f_i(t)\,dt\right).     \tag{IMP.5}
\]

Together with each T05 increment law, IMP.5 proves that \(F_A\) is a T05
witness for the integrand in IMP.4 on \(S_A\). The reverse direction of T08
then gives IMP.4. This proof handles refinement and the codomain change in one
semantic step and does not expose relation constructors or regular-point
insertion.

This single target deliberately subsumes the only finite-linearity operations
needed downstream. Taking \(A=\varnothing\) gives zero. If one integrand has
exceptional set \(S\) and a smooth model term has the empty exceptional set,
the result is correctly stated on \(S\); no consumer must separately invoke a
regular-point insertion lemma. Identity maps on a
two-element set give addition; the map \(v\mapsto-v\) gives negation; scalar
multiplication and, for \(E=\mathbb C\), multiplication by a fixed complex
coefficient are continuous real-linear maps. Taking \(E=\mathbb R,H=\mathbb C\)
or \(E=\mathbb C,H=\mathbb R\) also supplies the scalar-transport cases needed
by real/complex consumers. No infinite sum or limit-sum interchange is asserted.

<a id="target-imp-001-t04"></a>
## IMP-001-T04 - split or prepend a regular interval

Fix \(a\le k\), \(S\subset(k,\infty)\), and assume \(f\) is
`IntervalIntegrable` on \([a,k]\). Put

\[
 J=\int_a^k f(t)\,dt.
\]

The target is the equivalence

\[
 \operatorname{HasImp}(f,a,S,J+I)
 \quad\Longleftrightarrow\quad
 \operatorname{HasImp}(f,k,S,I).                        \tag{IMP.6}
\]

For the right-to-left implication, apply T08 to obtain a T05 witness \(F\) from
\(k\), with \(F(t)\to I\). Define a primitive from \(a\) by

\[
 G(t)=
 \begin{cases}
   \int_a^t f,&a\le t\le k,\\
   J+F(t),&t\ge k.
 \end{cases}                                             \tag{IMP.7}
\]

The two formulas agree at \(k\) because \(F(k)=0\). The prepended integrability
hypothesis and the standard continuity of a finite interval primitive give
continuity on \([a,k]\); T05 gives continuity on \([k,\infty)\). Adjacent
interval additivity proves the increment law across \(k\), while each side is
immediate from its defining primitive. Thus \(G\) satisfies T05 from \(a\) and
tends to \(J+I\); T08 gives the left side of IMP.6.

Conversely, T08 applied from \(a\) gives a primitive \(G\) tending to \(J+I\).
Since \(S\subset(k,\infty)\), its increment law on \([a,k]\) yields
\(G(k)=\int_a^k f=J\). Put \(F(t)=G(t)-G(k)\) for \(t\ge k\), with arbitrary
values below \(k\). Then \(F\) is a T05 witness from \(k\), is normalized, and
tends to \((J+I)-J=I\). T08 gives the right side. This proves IMP.6 with the
displayed order \(J+I\); no sign or orientation convention is hidden.

<a id="target-imp-001-t07"></a>
## IMP-001-T07 - absolute Bochner bridge for an arbitrary finite marking set

Assume `IntegrableOn f (Ioi k)` and \(k<c\) for every \(c\in S\). The target is

\[
 \operatorname{HasImp}
   \left(f,k,S,\int_{(k,\infty)}f(t)\,dt\right).         \tag{IMP.25}
\]

The `IntegrableOn` premise is essential: the set integral in the conclusion is
totalized by Mathlib, so its displayed value alone is never evidence of
convergence. The finite set \(S\) is arbitrary; its points are merely regular
marks for an absolutely integrable function.

Define the canonical primitive

\[
 P(t)=\int_k^t f(u)\,du.                                 \tag{IMP.26}
\]

For \(t\ge k\), `intervalIntegral.integral_Ioi_sub_Ioi` rewrites this as the
difference between the Ioi integral from \(k\) and the Ioi integral from
\(t\). The tail primitive is continuous on \([k,\infty)\) by
`MeasureTheory.IntegrableOn.continuousOn_Ici_primitive_Ioi`, so \(P\) is
continuous there and \(P(k)=0\). If \(k\le a\le b\), restriction of the
`IntegrableOn (Ioi k)` hypothesis to `Ioc a b`, together with
`intervalIntegral.intervalIntegrable_iff_integrableOn_Ioc_of_le`, gives honest
`IntervalIntegrable` evidence. Adjacent interval additivity gives

\[
 P(b)-P(a)=\int_a^b f.                                  \tag{IMP.27}
\]

Hence \(P\) satisfies T05 for every finite \(S\) above \(k\). Finally,
`MeasureTheory.intervalIntegral_tendsto_integral_Ioi` gives

\[
 P(t)\longrightarrow\int_{(k,\infty)}f
 \quad(t\to+\infty).                                   \tag{IMP.28}
\]

The reverse direction of T08 proves IMP.25. No extra regularity at the single
point \(k\) is required: the open-ray integrability premise ignores its value,
while the resulting interval integrals are unchanged by that null-set value.

<a id="target-imp-001-t06"></a>
## IMP-001-T06 - public finite-exception exponential composition

Let `IsFiniteExceptionalPrimitive g k S F` hold, fix arbitrary
\(h\in\mathbb R\), and put \(w(t)=e^{-ht}\). Assume the two explicit
weighted-tail propositions

\[
 t\longmapsto w(t)\mathbin{\bullet}F(t)
 \quad\hbox{is Bochner integrable on }(k,\infty),       \tag{IMP.12a}
\]

\[
 w(R)\mathbin{\bullet}F(R)\longrightarrow0
 \quad(R\to+\infty).                                   \tag{IMP.12b}
\]

The public target is

\[
 \operatorname{HasImp}
 \left(t\mapsto w(t)\mathbin{\bullet}g(t),\ k,\ S,\
   h\mathbin{\bullet}\int_{(k,\infty)}
     w(t)\mathbin{\bullet}F(t)\,dt\right).             \tag{IMP.12c}
\]

The set integral in IMP.12c is honest because IMP.12a is a premise. T06 has
no positivity premise on \(h\), bound on \(F\), or quantitative conclusion.
In QL-001, \(h>0\), \(L\ge0\), and
\(\|F(t)\|\le L\) are separate flat hypotheses used to derive IMP.12a and
IMP.12b; they do not become fields of T05. The conclusion exposes both the
target relation and its exact value, so a consumer need not eliminate or
unfold T01's private representation.

### Private finite-piece Abel lemma

Let \(a\le b\), let \(g:\mathbb R\to E\) be `IntervalIntegrable` on
\([a,b]\), and let \(F:\mathbb R\to E\) be continuous on \([a,b]\). Assume

\[
 F(t)-F(a)=\int_a^t g(v)\,dv
 \qquad(a\le t\le b).                                   \tag{IMP.12d}
\]

For the same arbitrary \(h\), the internal identity is

\[
 \int_a^b w(t)\mathbin{\bullet}g(t)\,dt
 =w(b)\mathbin{\bullet}F(b)-w(a)\mathbin{\bullet}F(a)
   +h\mathbin{\bullet}\int_a^b w(t)\mathbin{\bullet}F(t)\,dt. \tag{IMP.13}
\]

This is the revision-3 T06 mathematics, retained byte-for-byte in substance
but no longer a public target. Its proof is the analytic engine for IMP.12c.

### `IMP-001-I03` - finite triangular Fubini identity

Set \(G(t)=\int_a^t g(v)\,dv\). On the square \([a,b]^2\), consider

\[
 \mathcal K(t,v)=\bigl(\mathbf 1_{\{a\le v\le t\le b\}}w(t)\bigr)
        \mathbin{\bullet}g(v).
\]

The exponential is bounded on the compact interval: for example
\(|w(t)|\le M:=e^{|h|\max(|a|,|b|)}\). Therefore

\[
 \|\mathcal K(t,v)\|\le
 \mathbf 1_{[a,b]^2}(t,v)\,M\|g(v)\|.                \tag{IMP.14}
\]

Put \(\nu=\mathrm{volume}\mathbin{\restriction}\operatorname{uIoc}(a,b)\).
The exact premise required by `MeasureTheory.intervalIntegral_integral_swap`,
with its inner measure specialized to \(\nu\), is

\[
 \operatorname{Integrable}(\operatorname{uncurry}\mathcal K)
   \bigl((\mathrm{volume}\mathbin{\restriction}
          \operatorname{uIoc}(a,b))\mathbin{\mathrm{.prod}}\nu\bigr). \tag{IMP.14a}
\]

In Lean-shaped notation, the exact proposition is
`MeasureTheory.Integrable (Function.uncurry K)
((volume.restrict (Set.uIoc a b)).prod (volume.restrict (Set.uIoc a b)))`.

This is not abbreviated to separate sectionwise integrability. To prove
IMP.14a, `IntervalIntegrable g volume a b` gives integrability of
\(\lVert g\rVert\) with respect to \(\nu\), while \(\nu\) is a finite measure
with total mass \(b-a\). The norm bound IMP.14 therefore has finite integral
over the restricted product measure. The required measurability obligation is
precisely that `Function.uncurry K` is `AEStronglyMeasurable` with respect to
\(\nu\mathbin{\mathrm{.prod}}\nu\), not that it is globally
`StronglyMeasurable`. It follows measure-locally from the
`AEStronglyMeasurable` part of the integrability of \(g\) with respect to
\(\nu\), continuity of \(w\), measurability of the triangle, and continuity of
scalar multiplication. Thus no global measurability hypothesis is added. The
Bochner domination criterion proves exactly IMP.14a. Bochner Fubini `IMP-M06`
now applies; the indicator sections and the null endpoints convert its inner
restricted set integrals back to the displayed interval integrals, yielding

\[
 \int_a^b w(t)\mathbin{\bullet}G(t)\,dt
 =\int_a^b\left(\int_v^b w(t)\,dt\right)
       \mathbin{\bullet}g(v)\,dv.                       \tag{IMP.15}
\]

No infinite domain, conditional integral, or derivative of \(F\) occurs in
this interchange.

### `IMP-001-I04` - scalar exponential identity

Pinned `Real.hasDerivAt_exp` (`IMP-M10`) composed with the derivative of
\(t\mapsto-h t\) gives

\[
 \frac{d}{dt}e^{-ht}=-h e^{-ht}.
\]

The exponential and its displayed derivative are continuous, hence interval
integrable, on \([u,v]\). Apply
`intervalIntegral.integral_eq_sub_of_hasDerivAt_of_le` (`IMP-M07`) to this
derivative and multiply the result by \(-1\). This gives, without division by
\(h\),

\[
 h\int_u^v w(t)\,dt=w(u)-w(v)                           \tag{IMP.16}
\]

for every \(u\le v\). This remains true at \(h=0\), when both sides are zero.
Multiply IMP.15 by \(h\), use IMP.16, and use Bochner linearity:

\[
 \begin{aligned}
 h\mathbin{\bullet}\int_a^b w(t)\mathbin{\bullet}G(t)\,dt
 &=\int_a^b (w(v)-w(b))\mathbin{\bullet}g(v)\,dv\\
 &=\int_a^b w(v)\mathbin{\bullet}g(v)\,dv
   -w(b)\mathbin{\bullet}\int_a^b g(v)\,dv.             \tag{IMP.17}
 \end{aligned}
\]

By IMP.12d at \(t=b\), the last unweighted integral is \(F(b)-F(a)\).

### `IMP-001-I05` - constant part and final rearrangement

IMP.12d says \(F(t)=F(a)+G(t)\) on \([a,b]\). Since \(F\) and \(w\) are
continuous, the weighted primitive is `IntervalIntegrable`. Split its integral
into the constant and \(G\) parts. A second application of IMP.16 gives

\[
 h\mathbin{\bullet}\int_a^b w(t)\mathbin{\bullet}F(a)\,dt
 =(w(a)-w(b))\mathbin{\bullet}F(a).                     \tag{IMP.18}
\]

Adding IMP.17 and IMP.18 yields

\[
 h\mathbin{\bullet}\int_a^b w(t)\mathbin{\bullet}F(t)\,dt
 =\int_a^b w(t)\mathbin{\bullet}g(t)\,dt
  -w(b)\mathbin{\bullet}F(b)+w(a)\mathbin{\bullet}F(a). \tag{IMP.19}
\]

Rearranging IMP.19 is exactly IMP.13. This proves the private finite-piece
lemma. At no point was
\(F'(t)=g(t)\) assumed or derived pointwise; the only primitive information was
the finite increment law IMP.12d.

### `IMP-001-I06` - construct the transformed primitive

Define, for \(t\ge k\),

\[
 G(t)=w(t)\mathbin{\bullet}F(t)
   +h\mathbin{\bullet}\int_k^t
       w(u)\mathbin{\bullet}F(u)\,du,                   \tag{IMP.20}
\]

and assign arbitrary values below \(k\). The first term is continuous on
\([k,\infty)\). IMP.12a supplies integrability of the second integrand on
every finite subinterval, so its variable-endpoint primitive is continuous.
Because \(F(k)=0\), \(G(k)=0\).

Now take \(k\le a\le b\) with \([a,b]\cap S=\varnothing\). T05 gives
`IntervalIntegrable g` and the increment law on this regular piece. The
private identity IMP.13 and adjacent additivity for the weighted-primitive
integral yield

\[
 G(b)-G(a)=\int_a^b w(t)\mathbin{\bullet}g(t)\,dt.      \tag{IMP.21}
\]

The continuous scalar weight also preserves `IntervalIntegrable` for \(g\).
Thus \(G\) is an `IsFiniteExceptionalPrimitive` witness for \(w\bullet g\) on
the same literal \(S\). All finite-exception stitching is now owned by T08;
T06 neither reconstructs nor exposes the ordered endpoint representation.

### `IMP-001-I07` - pass the transformed primitive to infinity

By IMP.12b, the first term in IMP.20 tends to zero. By IMP.12a and pinned
`IMP-M11`,

\[
 \int_k^t w(u)\mathbin{\bullet}F(u)\,du
 \longrightarrow
 \int_{(k,\infty)}w(u)\mathbin{\bullet}F(u)\,du.        \tag{IMP.22}
\]

Continuous scalar multiplication and addition therefore show

\[
 G(t)\longrightarrow
 h\mathbin{\bullet}\int_{(k,\infty)}
    w(u)\mathbin{\bullet}F(u)\,du.                     \tag{IMP.22a}
\]

Apply the reverse implication of T08 to the T05 witness \(G\) and this limit.
The result is exactly IMP.12c for the original arbitrary finite \(S\). The
private Abel identity is the only finite-piece analytic engine; T08 is the only
component-stitching engine, so the proof has no circular dependence on T06.

## Analytic side-condition ledger

| ID | Exact proposition | Used by | Proof or dependency | Status |
|---|---|---|---|---|
| `IMP-A01` | \(S\subset(k,\infty)\) | T01--T08 | explicit predicate/target hypothesis | discharged conditionally |
| `IMP-A02` | every truncated interval used by T01/T08 stays in one regular component | T01, T08 | endpoint filters and ordered finite set | discharged |
| `IMP-A03` | `IntervalIntegrable f` on every regular truncation | T01, T05, T08 | explicit semantic clauses, inherited by T03/T04 constructions | discharged conditionally |
| `IMP-A04` | endpoint filters are nontrivial | T02 | strict component inequalities; midpoints and \(c+1\) lie inside | discharged |
| `IMP-A05` | continuous linear maps commute with finite Bochner integrals | T03 | exact pinned `IMP-M02` | discharged |
| `IMP-A06` | \(f\) is integrable on the prepended \([a,k]\) | T04 | explicit hypothesis | discharged conditionally |
| `IMP-A07` | \(F\) is continuous across each \(c\in S\) | T05, T08, T06-I06 | explicit predicate clause or constructed from separate limits | discharged conditionally |
| `IMP-A08` | \(g\) is integrable on \([a,b]\) | private finite-piece lemma | T05 on each regular piece | discharged |
| `IMP-A09` | \(wF\) is integrable on \([a,b]\) | private finite-piece lemma | continuity of \(w,F\), `IMP-M05` | discharged |
| `IMP-A10` | `MeasureTheory.Integrable (Function.uncurry K) ((volume.restrict (Set.uIoc a b)).prod (volume.restrict (Set.uIoc a b)))` | IMP-I03 | `AEStronglyMeasurable` under that exact restricted product measure, plus domination IMP.14; no global `StronglyMeasurable` premise | discharged |
| `IMP-A11` | \(t\mapsto e^{-ht}\) has derivative \(-he^{-ht}\), both functions are interval integrable, and FTC divides by no parameter | IMP-I04 | `IMP-M10` plus chain rule, then `IMP-M07` | discharged, including \(h=0\) |
| `IMP-A12` | \(wF\) is integrable on \((k,\infty)\) | T06-I06--I07 | explicit T06 hypothesis IMP.12a | discharged conditionally |
| `IMP-A13` | \(w(R)F(R)\to0\) atTop | T06-I07 | explicit T06 hypothesis IMP.12b | discharged conditionally |
| `IMP-A14` | no endpoint or whole-set integral is interpreted by totalization | all | regular-piece integrability, separate `Tendsto` clauses, IMP.12a for T06, and `IntegrableOn` for T07 | discharged by contract |
| `IMP-A15` | \(f\) is `IntegrableOn (Ioi k)` | T07 | explicit premise, restricted by `IMP-M14` | discharged conditionally |
| `IMP-A16` | the primitive built from T01 is continuous at every exceptional point and tends to \(I\) | T08 forward | the independent component limits and finite telescope in IMP.24a--IMP.24c | discharged |

The exact dependencies and defining imports are now bound in the pinned
dependency ledger. Future implementation must discharge the restricted-product
proposition in `IMP-A10` exactly as stated; proof plumbing may change, but these
side conditions may not be weakened.

## Exact identity and loss ledger

IMP-001 contains no inequality target and loses no constant. For completeness,
the exact transformations are recorded in the schema's loss-ledger shape.

| ID | Expression before | Applied fact | Side conditions | Expression after | Natural or weakened |
|---|---|---|---|---|---|
| `IMP-L01` | component split at anchor \(d\) | IMP.2 | regular-piece integrability | split at anchor \(e\) with cancelling fixed integral | exact |
| `IMP-L02` | integral of finite mapped sum | `IMP-M02` and finite linearity | every summand integrable | finite mapped sum of integrals | exact |
| `IMP-L03` | integral from \(a\) to a truncation | `IMP-M01` at \(k\) | \(a\le k\), integrable on \([a,k]\) | \(J+\) tail truncation | exact |
| `IMP-L04` | triangular iterated integral | Bochner Fubini | IMP.14 | reversed iterated integral | exact |
| `IMP-L05` | \(h\int_u^v e^{-ht}dt\) | exponential FTC | \(u\le v\) | \(e^{-hu}-e^{-hv}\) | exact |
| `IMP-L06` | IMP.19 | additive rearrangement | none | Abel identity IMP.13 | exact |
| `IMP-L07` | T05 witness \(F\) and private Abel identity | definition IMP.20 and finite additivity | T05 and IMP.12a | transformed T05 witness \(G\) with increment IMP.21 | exact |
| `IMP-L08` | transformed primitive \(G\) | IMP.12b, `IMP-M11`, and T08 reverse | explicit T06 tail hypotheses | exact limit IMP.22a and relation IMP.12c | exact |
| `IMP-L09` | component values of T01 | cumulative definitions IMP.24a--IMP.24c | independent one-sided limits | T05 witness tending to the same \(I\) | exact |
| `IMP-L10` | `IntegrableOn f (Ioi k)` | canonical primitive IMP.26 and T08 reverse | pinned IMP-M12--IMP-M14 | relation at the honest Ioi integral | exact |

## Boundary and adversarial cases

### Empty exceptional set

When \(S=\varnothing\), T01 is precisely a right-endpoint limit of honest
`intervalIntegral` values from \(k\), with integrability on every compact
\([k,R]\). T02--T04 reduce to uniqueness, finite linearity, and additivity for
that ordinary one-sided improper integral. T06 reduces to the private
finite-piece identity followed by IMP.12a--IMP.12b at infinity; it still
constructs the direct relation without a representation-specific base-case
constructor. T08 reduces to the familiar equivalence with a normalized
primitive having a finite limit, and T07 is the standard absolute-integral
special case.

### Initial-endpoint singularities

An endpoint singularity proportional near \(k\) to \((t-k)^{-1/2}\), with an
integrable tail such as an exponential cutoff, is `IntegrableOn (Ioi k)` and is
accepted by T07 and T01; its value at \(k\) is irrelevant. By contrast, a
merely conditionally improper but non-L1 singularity at \(k\) cannot satisfy
T01's `IntervalIntegrable` clauses and cannot enter through T07. Conditional
finite singularities remain supported only at explicitly marked points of
\(S\), where both one-sided limits must exist independently.

### Principal-value-only singularity is rejected

Let \(c>k\), multiply \(t\mapsto1/(t-c)\) by a continuous cutoff equal to one
near \(c\), and put \(S=\{c\}\). Symmetric cutoffs around \(c\) can cancel and
produce a Cauchy principal value, but the left and right logarithmic integrals
diverge separately. T01 rejects the function. Any definition based on one
coupled pair of endpoints would incorrectly accept it and is a negative
regression.

### A genuinely conditional finite singularity is accepted

Near \(c>k\), take a cutoff times

\[
 t\longmapsto\frac{\sin(1/(t-c))}{t-c}.
\]

On either side, \(u=1/(t-c)\) converts the endpoint integral into a tail of
\(\sin u/u\), which converges by Dirichlet but not absolutely. Both one-sided
limits exist independently, so T01 accepts the example with \(S=\{c\}\).
Its accumulated primitive extends continuously through \(c\), so T05 can also
represent it. A global local-Bochner-integrability premise would wrongly reject
this intended case.

### Conditional convergence at infinity is not strengthened

For \(k>0\), \(f(t)=\sin(t)/t\) has an ordinary right-ray improper integral but
is not norm-integrable. T01 accepts it with \(S=\varnothing\). The packet never
claims that Mathlib's whole-set Bochner integral exists or equals its relational
value.

### Totalized nonintegrable finite pieces cannot fake convergence

Mathlib's integral expression is total even when an integrability proposition
is false. A predicate that mentioned only convergence of those values could
accept a meaningless constant sequence of totalized values. T01 separately
requires `IntervalIntegrable` on every regular truncation, so such a function
fails before its endpoint values are considered. The pinned negative witness
is `intervalIntegral.integral_undef` (`IMP-M08`), which makes the danger
literal: a nonintegrable finite interval integral is definitionally forced to
zero rather than left undefined.

### Degenerate finite interval and zero exponential parameter

If \(a=b\), every integral in the private identity IMP.13 is zero and the two
boundary terms cancel. If \(h=0\), IMP.13 reduces to
\(\int_a^b g=F(b)-F(a)\), exactly IMP.12d at \(b\). Public T06 also permits
\(h=0\) whenever IMP.12a says \(F\) is integrable on the ray and IMP.12b says
\(F(R)\to0\); its exact related value is then zero. No proof divides by \(h\).

### Nondifferentiable primitive

Let \(k=a<m<b\), take \(g\) to be the indicator of \([m,b]\), extended by zero
after \(b\), and set \(F(t)=\int_a^t g\). Then \(F\) is continuous, normalized,
and satisfies the T05 increment law but is not differentiable at \(m\). For any
\(h>0\), \(F\) is bounded and eventually constant, so IMP.12a and IMP.12b hold.
Public T06 applies, as does its private finite-piece lemma across \(m\). A future
proof that assumes `HasDerivAt F g` everywhere would be a strictly stronger and
rejected target.

### Continuity across an exceptional point is essential

Let \(g=0\) everywhere, let \(F=0\) on the regular component
to its left, and let \(F=1\) on the regular component to its right. The
regular-piece increment law holds on each component, but \(F\) is
discontinuous at \(c\). The improper integrals of \(g\) on both components are
zero, while the two Abel boundary terms at \(c\) differ by \(w(c)\) and do not
cancel. Thus componentwise increment laws without continuity are insufficient
for the exceptional-point composition used by QL-001. T05 explicitly rules
out this false weakening.

### Endpoint equality does not replace the increment law

On \([0,1]\), take \(g=0\) and \(F(t)=t(1-t)\). Then
\(F(1)-F(0)=\int_0^1 g=0\), so the increment identity holds at the two outer
endpoints only. For \(h>0\), however, the left side and boundary terms of
IMP.13 vanish while
\(h\int_0^1e^{-ht}F(t)\,dt>0\). Therefore the private lemma, and hence public
T06 through T05, genuinely needs the increment law for every intermediate
\(t\), not just at \(b\).

### Regular split sign

For a constant nonzero integrand on a finite prepended segment and any
convergent tail, T04 must add \(\int_a^k f\), not subtract it. Reversing \(a,k\)
without changing the interval-integral orientation is outside the target.

## Hypothesis- and choice-laundering audit

| Hypothesis/data | Why it is legitimate | What it must not conceal |
|---|---|---|
| `CompleteSpace E` | required for the Bochner/Fubini route | no scalar Watson or Gamma assumption |
| finite \(S\subset(k,\infty)\), or the family \(S_i\) | explicit domain bookkeeping | no order-dependent hidden selection; T03 uses their literal finite union |
| regular-piece `IntervalIntegrable` | prevents totalization and justifies additivity | no integrability across a singularity |
| independent endpoint limits | definition of ordinary improper convergence | no principal-value cancellation |
| continuous \(F\) | glues separate finite endpoint limits and makes \(wF\) integrable on compact intervals | no global integrability, bound, or limit at infinity |
| \(F(k)=0\) | explicit normalization | no existence or uniqueness by opaque choice |
| increment law IMP.9/IMP.12d | exact primitive information consumed by Abel | no derivative and no target weighted integral |
| weighted integrability IMP.12a | makes the explicit T06 value honest and controls its finite-interval limit | no bound on \(F\) and no target relation |
| boundary vanishing IMP.12b | removes only the terminal Abel boundary term | no selected limit or quantitative estimate |
| `IntegrableOn f (Ioi k)` | makes T07's Bochner set integral honest | no inference from a totalized value and no converse from T01 |
| `∃ F` in T08 | expresses the ordinary accumulated-primitive semantics | no fixed arbitrary witness, public `Classical.choose`, or norm bound |
| \(a\le b\), \(a\le k\) | fixes orientation in the private finite-piece lemma and T04 | no silently reversed interval |

There is no source hypothesis to discharge. There is also no choice of a
target function, baseline weight, truncation order, exceptional set, or
improper value. \(S\), every \(S_i\), and every T05/T06 \(F\) are explicit
inputs. T08 asserts existence of a primitive but defines no chosen primitive.
The T03 output set is the ordinary finite union \(S_A\), not a chosen superset.
Sorting a finite set is canonical. The displayed component anchors are deterministic, and IMP-I01
proves the result independent of them. Component limits are unique by T02.
If a later ergonomic API defines a value from an existence proof, that would be
a separate choice-bearing packet and could use T02; IMP-001 itself does not.

## Regression obligations

| ID | Planned regression | Protects |
|---|---|---|
| `IMP-R01` | T01 binder order fixes \(E,f,k,S,I\), with \(S\subset(k,\infty)\) visible | signatures and domain |
| `IMP-R02` | empty \(S\) agrees with the direct right-endpoint limit | base semantics |
| `IMP-R03` | T08 is checked for empty \(S\) and for multiple exceptional points, with every independent one-sided value and the exact finite telescope | complete component semantics |
| `IMP-R04` | (1/(t-c)) with only symmetric PV fails | independent one-sided limits |
| `IMP-R05` | cutoff \(\sin(1/(t-c))/(t-c)\) succeeds but is not globally locally Bochner integrable | conditional finite singularity coverage |
| `IMP-R06` | \(\sin t/t\) succeeds at infinity without an absolute-integrability conclusion | source-faithful conditional semantics |
| `IMP-R07` | a nonintegrable regular truncation fails even if its totalized integral expression has a value | integrability evidence |
| `IMP-R08` | T03 combines an amplitude on \(S\) with smooth model terms on \(\varnothing\), and tests empty, two-term, negation, complex multiplication, \(\mathbb R\to\mathbb C\), and \(\mathbb C\to\mathbb R\) CLM instances | finite-union refinement, exact linearity, and the generalized \(E\to H\) codomain |
| `IMP-R09` | T04 tests \(a=k\) and a constant prepended segment | split orientation and zero length |
| `IMP-R10` | T05 exposes continuity, normalization, and increment law but no bound \(L\) | hypothesis laundering |
| `IMP-R11` | a discontinuous componentwise primitive fails exceptional telescoping | continuity at \(S\) |
| `IMP-R12` | endpoint-only equality with \(g=0,F(t)=t(1-t)\) fails the private finite-piece lemma and cannot populate T05 for T06 | all-intermediate-point increment law |
| `IMP-R13` | the private lemma tests \(a=b\) and \(h=0\); public T06 tests \(h=0\) under IMP.12a--IMP.12b and the bounded eventually constant indicator primitive for \(h>0\) | endpoint algebra and no derivative premise |
| `IMP-R14` | exceptional Abel composition never treats a nonintegrable prefix as one `intervalIntegral` | split-improper LHS |
| `IMP-R15` | import and axiom audit finds no QL, Olver, Gamma, QB, or DEF dependency | downward-only architecture |
| `IMP-R16` | prospective declarations do not appear in Lean before external `lean_ready` | proof-before-Lean gate |
| `IMP-R17` | T03 constructs the finite mapped-sum primitive after weakening each T05 witness from \(S_i\) to the literal union \(S_A\), then invokes T08 reverse | finite-union refinement without representation dependence |
| `IMP-R18` | the private finite-piece Fubini witness has type `MeasureTheory.Integrable (Function.uncurry K) ((volume.restrict (Set.uIoc a b)).prod (volume.restrict (Set.uIoc a b)))`, with `AEStronglyMeasurable` proved only under that measure and no global `StronglyMeasurable` hypothesis | exact restricted-product premise without hypothesis laundering |
| `IMP-R19` | derive \((e^{-ht})'=-he^{-ht}\) from `Real.hasDerivAt_exp`, then prove IMP.16 also at \(h=0\) | exact exponential dependency and no division |
| `IMP-R20` | in a disposable downstream file importing only the public IMP modules, quantify arbitrary finite \(S\), assume T05, \(h>0\), \(L\ge0\), and \(\|F(t)\|\le L\) on \([k,\infty)\); derive IMP.12a--IMP.12b and invoke only public T06 to construct IMP.12c, without unfolding T01 or copying private recursion | representation-independent QL reachability and the Basic-to-Abel-to-QL dependency boundary |
| `IMP-R21` | T07 succeeds for every finite \(S\subset(k,\infty)\) from `IntegrableOn (Ioi k)`, while a totalized Ioi integral value without `IntegrableOn` cannot invoke it | honest Bochner bridge |
| `IMP-R22` | an exponentially cut off \((t-k)^{-1/2}\) endpoint singularity succeeds, while a conditionally improper non-L1 singularity at \(k\) fails the T01/T07 local contract | initial-endpoint semantics |
| `IMP-R23` | T08 requires an existential primitive, rejects replacing that existential by an arbitrary fixed \(F\), gives no norm bound, and is invariant under changes to \(F\) below \(k\) | exact equivalence quantifiers and no laundering |
| `IMP-R24` | changing values of \(f\) at \(k\) and at points of \(S\) preserves T01, T07, and T08 | null-set endpoint values |
| `IMP-R25` | OLV supplies T01 at the baseline, uses T08 forward, and derives a global norm bound only from the resulting continuity and finite atTop limit | no bound bundled in T08 |

### QL-shaped acceptance regression `IMP-R20`

The eventual disposable compile test has exactly the public IMP imports
`LMLF.Integral.Improper.Basic` and `LMLF.Integral.Improper.Abel`; it imports no
QL, source, audit, or implementation-helper module. It quantifies an arbitrary
`S : Finset ℝ`, not just the empty or singleton cases. For arbitrary
\(g,k,S,F,h,L\), it assumes

1. public T05, `IsFiniteExceptionalPrimitive g k S F`;
2. \(h>0\) and \(L\ge0\); and
3. the separate flat hypothesis \(\|F(t)\|\le L\) for every \(t\ge k\).

The test bounds
\(\|e^{-ht}F(t)\|\le Le^{-ht}\) on \((k,\infty)\). Integrability of the scalar
exponential majorant gives IMP.12a, and the same estimate plus \(h>0\) gives
IMP.12b. It then invokes the public T06 name and constructs

\[
 \operatorname{HasImp}
 \left(t\mapsto e^{-ht}\mathbin{\bullet}g(t),\ k,\ S,\
   h\mathbin{\bullet}\int_{(k,\infty)}
      e^{-ht}\mathbin{\bullet}F(t)\,dt\right).          \tag{IMP.R20}
\]

The test is rejected if it unfolds `HasImproperIntegralAtTopExcept`, mentions
an ordered-break or tail-recursion helper, uses `change` to restate a private
base/step clause, or copies either private recursion. The constructed witness
must remain consumable through public T02--T04. This regression is the
acceptance condition that directly closes exact-signature finding B1; examples
for fixed empty or singleton \(S\) do not substitute for it.

Expected negative elaboration tests must show that T01 cannot be constructed
without the regular-piece `IntervalIntegrable` fields, that a principal-value
witness does not satisfy either one-sided limit, and that T06 cannot be applied
from endpoint equality alone without T05's all-regular-piece increment law.
They must also show that T07 cannot be applied to a merely totalized
nonintegrable Ioi integral, and that T08 cannot be specialized to a fixed
arbitrary primitive in place of its existential witness.

## Exact QL/OLV consumer map

The downstream route is fully public and has no reverse import:

| Consumer step | Public IMP target | Exact output consumed next |
|---|---|---|
| Each absolutely integrable Gamma/model term | T07 in `Improper.Bochner` | T01 on the consumer's arbitrary finite \(S\), at the honest Ioi integral |
| Subtract the finite model from the source amplitude | T03 in `Improper.Basic` | T01 for the remainder on the literal union of exceptional sets, with codomain transport \(E\to H\) available |
| Remove the proper local interval \([0,k_n]\) | T04 in `Improper.Basic` | T01 for the baseline-weighted tail starting at \(k_n\) |
| Expose the source-faithful primitive | T08 forward in `Improper.Basic` | some explicit T05 witness \(F_n\) with `Tendsto F_n atTop (nhds I_n)` |
| Obtain the quantitative primitive premise | elementary topology outside IMP | boundedness of \(F_n\) from continuity on the finite prefix plus its finite atTop limit; no bound comes from T08 |
| Change from baseline weight \(X\) to target weight \(x\) | T06 in `Improper.Abel` | T01 for the target-weighted tail at the exact transformed value |

QL consumes the first, second, third, and sixth rows to prove its reusable
finite estimate. OLV supplies the canonical source hypothesis in T01 form and
uses the fourth and fifth rows; it does not expose raw endpoint witnesses in
its public hypothesis. `Basic`, `Bochner`, and `Abel` import no QL or OLV file,
so the route is acyclic.

## Trust, imports, and exclusions

The intended implementation splits prospectively into three modules.
`LMLF.Integral.Improper.Basic` owns T01, T05, T08, and T02--T04 plus private
finite-order and endpoint representation. `LMLF.Integral.Improper.Bochner`
imports `Basic` and `Mathlib.MeasureTheory.Integral.IntegralEqImproper` and owns
T07. `LMLF.Integral.Improper.Abel` imports `Basic`, the finite-product/Fubini and
interval FTC support, the real exponential derivative support, and
`IntegralEqImproper`; it owns T06 and the private finite-piece identity. Abel
does not import Bochner merely to reuse T07, because its transformed primitive
already comes with the stronger explicit weighted-integrability hypothesis.

No IMP module may import `LMLF.Integral.Watson` or any `LMLF.Olver1997` module.
The public boundary is exactly the eight targets listed above. Ordered
components, endpoint filters, recursive constructors and eliminators, the
optional regular-point-insertion calculation, the finite-piece identity,
triangle indicators, and set-integral passage plumbing remain private. The
downstream regressions use only public targets and neither name nor reproduce
those helpers.

The argument uses ordinary classical mathematics already admitted by Lean and
Mathlib. Finite sorting may be noncomputable at the implementation level, but
noncomputability is not an extra axiom and is not used to choose an integral
value. A release audit must report `#print axioms` for theorem targets and the
transitive imports.

This packet contains no floating-point evaluation, interval arithmetic,
quadrature, tolerance, adaptive truncation, optimizer, numerical solver, or
external numerical certificate. It introduces no Riemann integral, principal
value, whole-set conditional integral, or improper-integral value function.

## Reconstruction of each target

- T01 quantifies \(E,f,k,S,I\) in that order, requires \(S\subset(k,\infty)\),
  regular-piece integrability and all independent endpoint limits, and relates
  their finite sum to \(I\).
- T02 takes two T01 witnesses for the same \(E,f,k,S\) and concludes equality
  of their explicit values.
- T03 takes finitely many T01 witnesses with one common \(k\) and individual
  finite sets \(S_i\subset(k,\infty)\), refines each witness to their literal
  finite union, and concludes T01 in a possibly different complete normed
  output space \(H\) for the finite \(E\to H\) continuous-real-linear
  combination with exactly the corresponding combined value.
- T04 takes \(a\le k\), a regular-piece integral \(J=\int_a^k f\), and proves
  the exact equivalence between the value \(I\) from \(k\) and \(J+I\) from
  \(a\).
- T05 takes explicit \(g,k,S,F\) and records only continuity, normalization,
  regular-piece integrability, and the increment law.
- T06 takes explicit \(g,k,S,F,h\), a T05 witness, integrability of \(wF\) on
  \((k,\infty)\), and vanishing of \(w(R)F(R)\) at infinity. For arbitrary real
  \(h\) satisfying those propositions it concludes T01, on the same literal
  \(S\), for \(wg\) at the exact value \(h\int_{(k,\infty)}wF\). The finite-piece
  identity IMP.13 is private proof infrastructure, not a public target.
- T07 takes `IntegrableOn f (Ioi k)` and an arbitrary finite
  \(S\subset(k,\infty)\), and concludes T01 at the honest Bochner Ioi integral.
- T08 states T01 if and only if there exists a T05 witness tending to \(I\). It
  includes no norm bound and makes no assertion about a fixed arbitrary \(F\).

No target has a stronger hidden premise or a smaller domain than its card.

## Remaining blockers to freezing

1. Obtain the required fresh independent approvals of the complete revision-5
   proof and structural architecture. They must cover T01/T05/T08 semantic
   equivalence, generalized T03, T04, T07, T06, dependency direction, target
   minimality, and the exact QL/OLV consumer route.
2. Only after those approvals, design an R5 exact Lean signature artifact,
   binder order, and minimal three-module import closure. The unchanged
   revision-3 signature bytes and approvals do not count for this gate.
3. Review the new exact signatures independently, including compiled forms of
   IMP-R20--R25 that import only the applicable public IMP modules and use no
   unfolding or copied private recursion.
4. Obtain the required structural-circularity gate and reconcile the currently
   unregistered ID with the registry and an execution manifest only after the
   preceding reviews accept frozen bytes.

No Lean implementation may begin while these blockers remain.
